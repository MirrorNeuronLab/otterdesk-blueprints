# Prompt assets

The conversation contract is defined next to the domain normalization logic in
`domain/conversation.py`. This directory is retained because the standard
MirrorNeuron blueprint bundle uploads a conventional `payloads/prompts` path for
every actor-backed workflow.

