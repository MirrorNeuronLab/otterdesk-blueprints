# Knowledge boundary

This internal co-worker does not load a separate knowledge base. Its only
evidence is the bounded read-only MCP snapshot supplied for the current desktop
question. The conventional directory is kept so the standard blueprint bundle
layout remains complete.

