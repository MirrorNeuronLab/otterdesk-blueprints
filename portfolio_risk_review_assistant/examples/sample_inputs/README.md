# Portfolio Risk Demo Inputs

`sample_portfolio.json` is a small review pack with holdings, benchmark targets, concentration limits, market signals, and the decision constraints that the assistant should use when generating a portfolio-risk review.

The numbers are synthetic and intentionally compact. They are enough to exercise the input contract, Monte Carlo settings, policy checks, and report output without depending on private brokerage exports.
