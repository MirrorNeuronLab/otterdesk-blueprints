# Prompt payload

This co-worker uses its structured agent and step definitions. This directory
is staged because the standard DockerWorker runtime mounts the `prompts`
payload for every stateful-step agent.
