# Microduck Controller sample input

This service has no file input. Open the published `/?control=1` URL in one
browser tab to create the simulation authority, then connect an MCP client to
the matching `/mcp` endpoint.
